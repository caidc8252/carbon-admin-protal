# Carbon Admin · 数据模型差异分析（新 PARTNER 模型 vs 现有 Entity 模型）

> 日期：2026-06-01
> 对比双方：
> - **新模型** —— 业务方提供的 `PARTNER` 系列数据定义（PARTNER / PARTNER CONTRACT RELATIONSHIP / PARTNER CONTRACT EVENT / PARTNER CONTRACT DELEGATION / ROLE / PARTNER ROLE BLOCKLIST / USER / MFA INFO / PARTNER USER RELATIONSHIP / OPERATOR INVITE）
> - **现有模型** —— 本仓库真源：`data-tables.jsx`（9 表 seed）+ `data.jsx`（派生层）+ `公共约束.md` v0.5 + `spec.md` v0.4
> 性质：**差异梳理 + 改造决策记录**。本文不含代码改动，作为后续改造的依据。

---

## 0. 决策记录（业务方已拍板）

| # | 议题 | 决策 | 取向 |
|---|---|---|---|
| 1 | PILOT 建模 | **采用新模型** | PILOT 作为**契约类型**（`ISO-PILOT` / `ISV-PILOT`）；契约 STATUS 收敛为 `ACTIVE / SUSPENDED / TERMINATED` |
| 2 | MERCHANT entitlements | **要做** | 新增 Payment Service + E-Receipt Service 条款及其编辑 / 计费口径 |
| 3 | 密码历史条数 | **保留 5 条** | 沿用现状，不改为 3（注：是「条数」不是「天数」） |
| 4 | 邀请 Token 有效期 | **保留 7 天** | 沿用现状，不改为 72 小时 |
| 5 | MFA 状态编码 | **改为字符串枚举** | 由 int `0/1/2` 改为字符串 `ENABLED / PENDING`（+ 视需要保留 DISABLED 映射） |
| 6 | USER 状态 | **新增 DISABLED** | 由 `ACTIVE / LOCKED` 扩为 `ACTIVE / LOCKED / DISABLED` |

| 7 | 契约 LOGOS（平台 Logo + App 启动 Logo） | **本期不做，延后** | 等其余改造完成后再启动；需主动提醒业务方 ⏰ |
| 8 | PARTNER USER RELATIONSHIP 的 `EXPIRED` 状态 | **去掉** | 不保留独立 EXPIRED 态，改由其他字段组合（如 AUTHORIZING TO 到期）派生表达 |
| 9 | PILOT 转正流程 | **采用建议方案** | 终止 `*-PILOT` 契约 + 同主体新建正式契约（`CREATE_CONTRACT` + `TERMINATE`） |
| 10 | MFA DISABLE 态 | **去掉** | MFA INFO STATUS 仅保留字符串 `ENABLED / PENDING`，不保留 DISABLE 映射 |

---

## 1. 命名层：`Entity` → `Partner`（全表改名）

新模型统一以 **PARTNER** 为核心，对应关系如下。语义不变，但 UI 中 "Customer / Partner / Entity" 三词口径需统一。

| 新模型 | 现有代码（真源） |
|---|---|
| PARTNER | `Entity` (`SEED_ENTITIES`) |
| PARTNER CONTRACT RELATIONSHIP | `EntityContract` (`SEED_ENTITY_CONTRACTS`) |
| PARTNER CONTRACT EVENT | `EntityContractEvent` |
| PARTNER CONTRACT DELEGATION | `EntityContractDelegation` (`SEED_ENTITY_CONTRACT_DELEGATIONS`) |
| PARTNER ROLE BLOCKLIST | `EntityRoleBlocklist` (`SEED_ROLE_BLOCKLIST`) |
| PARTNER USER RELATIONSHIP | `EntityUserRelationship` (`SEED_ENTITY_USER_RELATIONSHIPS`) |
| ROLE | `Role` (`SEED_ROLES`) |
| USER | `User` (`SEED_USERS`) |
| MFA INFO | `MFAInfo` (`SEED_MFA_INFO`) |
| OPERATOR INVITE | `OperatorInvite` (`SEED_OPERATOR_INVITES`) |

> 现有 UI 对外仍称 "Customer"（`SEED_CUSTOMERS` = 非 NPT 的派生视图）。改名时需保留这套对外称呼或一并梳理。

---

## 2. 🔴【架构级】PILOT：契约类型化（决策 #1，采用新模型）

这是两套模型唯一不兼容的根本分歧，已决定采用**新模型**。

### 2.1 两套做法对照

| | 新模型（采用） | 现有代码（待改造） |
|---|---|---|
| PILOT 表达 | 独立 **CONTRACT TYPE**：`ISO-PILOT` / `ISV-PILOT` | 契约 **STATUS = `PILOT`** + 派生态 `EXPIRED` |
| 契约 STATUS 枚举 | `ACTIVE / SUSPENDED / TERMINATED` | `PILOT / ACTIVE / SUSPENDED / TERMINATED`（+派生 EXPIRED） |
| "是否曾经试用" | 按 PARTNER ID 聚合所有 `*-PILOT` 类型契约 | `events.any(eventType == 'BIND_PILOT')` |
| 试用免费 | `*-PILOT` 类型契约所有服务费强制为 0 | PILOT 状态下费用锁定为 0 |
| 转正 | （新模型未显式定义流程）→ 需约定为：终止 `ISO-PILOT` + 新建 `ISO`（或改 type） | 同一行 `PILOT → ACTIVE` Convert（`PILOT_CONVERT` 事件） |
| 续期 | `RENEW` 事件改 EFFECTIVE TO | `PILOT_EXTEND` 事件，+30/60/90 天 |

### 2.2 改造影响清单

1. **枚举常量**
   - `CONTRACT_TYPES`：`['ADMIN','ISO','ISV','MERCHANT']` → 增加 `'ISO-PILOT'`、`'ISV-PILOT'`、`'DISTRIBUTOR'`。
   - `CONTRACT_STATUSES`：`['PENDING','ACTIVE','SUSPENDED','TERMINATED']` → **`['ACTIVE','SUSPENDED','TERMINATED']`**（同时去掉历史遗留的 `PENDING`，去掉 `PILOT`）。
2. **Seed 数据**：`ec-015-iso` / `ec-016-iso` 当前是 `type:'ISO'` + `status:'PILOT'` → 改为 `type:'ISO-PILOT'` + `status:'ACTIVE'`，EFFECTIVE TO 不变。
3. **事件词表**（见 §4）：去掉 `BIND_PILOT` / `PILOT_EXTEND` / `PILOT_CONVERT`，改用新模型词表。
4. **派生逻辑**：`getEntityView` 中所有判断 `status==='PILOT'` 的地方改为判断 `contractType` 是否以 `-PILOT` 结尾。
5. **UI**：契约卡片标签、契约向导（试用选项）、客户列表状态 chip、"试用转正"流程，全部按"类型"而非"状态"重写。
6. **转正流程**（决策 #9，采用建议方案）：「`ISO-PILOT` 契约置 TERMINATED + 同主体新建正式 `ISO` 契约（带正式收费条款）」，事件用 `CREATE_CONTRACT`（新契约）+ `TERMINATE`（旧 pilot 契约）组合表达。

---

## 3. 🟠 契约表（PARTNER CONTRACT RELATIONSHIP）字段差异

| 字段 | 新模型 | 现有代码 | 处理 |
|---|---|---|---|
| `AUTHORIZED CONTRACT TYPE` | `ISO / ISV / ISO-PILOT / ISV-PILOT / MERCHANT / DISTRIBUTOR / ADMIN` | `ADMIN / ISO / ISV / MERCHANT`（+ 代码层 Distributor 不一致） | 扩枚举（含 *-PILOT + DISTRIBUTOR），并补齐 `data-tables.jsx` 与 `data.jsx` 的不一致 |
| `AUTHORIZING PARTNER CONTRACT TYPE` | **新增**冗余字段（授权方所用契约类型，快速识别 ADMIN 授权 / ISO 授权） | 无（靠 `authorizingEntityId` 反查） | 新增冗余字段，回填 seed |
| `STATUS` | `ACTIVE / SUSPENDED / TERMINATED` | `PILOT / ACTIVE / SUSPENDED / TERMINATED` | 收窄（见 §2） |
| `ENTITLEMENTS` (MERCHANT) | **有内容**（Payment + E-Receipt，见 §3.1） | `{}` 空 | 🔴 新增（决策 #2） |
| `LOGOS` (JSONB) | **新增**（平台 Logo + App 启动 Logo） | 无 | ⏰ **本期不做**（决策 #7），延后；待提醒 |

### 3.1 🔴 MERCHANT entitlements（决策 #2，要做）

新模型 MERCHANT 契约条款（现有为 `{}`，需新增）：

```jsonc
// MERCHANT entitlements
{
  "PAYMENT SERVICE": {
    "enable": true,
    "appItems": [
      { "pkgName": "com.newland.payexplorer", "price": 0 }   // x per device per month；enable=true 时不可空，0=免费
    ]
  },
  "E-RECEIPT SERVICE": {
    "enable": true,
    "price": 0,                       // x per month；enable=true 时不可空，0=免费
    "options": ["EMAIL", "QR CODE"]   // 可选服务
  }
}
```

计费口径：Payment Service 按 **设备/月**；E-Receipt 按 **月**。需要：
- Merchant 契约的 entitlements 编辑 UI（区别于 ISO 的设备/增值服务面板）。
- Merchant 契约卡片展示这两类服务的开关 + 月费。
- seed 数据为现有 MERCHANT 契约（`c-007/008/010/013/014`）回填合理默认值。

### 3.2 ISO entitlements（✅ 基本一致）

新旧均为：`deviceModels / settlementCurrency / deviceBasicService / FlyDesk / GeoLocation / GeoFencing / Pre-warning`，且都含「GeoFencing 依赖 GeoLocation」约束。现有 seed 已是这套形状，**无需改动**。

### 3.3 ⏰ LOGOS（本期不做，延后 — 决策 #7）

> **提醒事项**：本项已商定延后。其余改造完成后需主动提醒业务方启动 LOGOS 改造。

```jsonc
// LOGOS (JSONB)
{
  "platformLogo": [ { "width": 0, "height": 0, "fileSize": 0, "url": "" } ],  // 管理平台个性化，数组
  "appBootLogo":  {  "width": 0, "height": 0, "fileSize": 0, "fileMd5": "", "url": "" }  // 终端 App 启动图，仅 ISO / ISO-PILOT 生效
}
```

现有模型完全无 Logo 概念。若本期做，需新增上传组件 + 契约详情的 Logo 管理区。

---

## 4. 🟠 契约事件表（PARTNER CONTRACT EVENT）词表差异

| 新模型 EVENT TYPE | 现有 eventType | DESCRIPTION 载荷（新模型） |
|---|---|---|
| `CREATE_CONTRACT` | `BIND_PILOT` / `BIND_ACTIVE` | `{ initialEffectiveTo, entitlementsSnapshot }` |
| `EDIT_CONTRACT` | `ENTITLEMENTS_EDIT` | `{ fromEntitlements, toEntitlements, reason? }` |
| `RENEW` | `PILOT_EXTEND` | `{ fromEffectiveTo, toEffectiveTo, reason? }` |
| `SUSPEND` | `SUSPEND` | `{ reason }` |
| `RESUME` | `RESUME` | `{ reason }` |
| `TERMINATE` | `TERMINATE` | `{ reason }` |
| —（无，转正用 CREATE+TERMINATE 表达） | `PILOT_CONVERT` | — |

改造：统一到新模型 6 个事件类型，移除 pilot 专属事件；现有 seed 的 `events[]`（含 `eventType:'BIND_PILOT'` 等）需重写。派生信息「续期次数 = count(RENEW)」「是否试用 = 看是否持有 *-PILOT 契约」。

---

## 5. 🟡 USER / MFA / 关系 / 邀请 差异

| 项 | 新模型 | 现有代码 | 决策 / 处理 |
|---|---|---|---|
| **USER.STATUS** | `ACTIVE / LOCKED / DISABLED` | `ACTIVE / LOCKED` | **新增 DISABLED**（决策 #6）：补停用/启用动作与 UI |
| **USER.PASSWORD HISTORY** | 最多 3 | 最多 5（`historySize:5`） | **保留 5**（决策 #3，沿用现状） |
| **USER.mfaEnable** | 无（仅 MFA INFO 表表达） | USER 上有 `mfaEnable` bool | 倾向以 MFA INFO 为准；`mfaEnable` 可作派生/兼容字段保留 |
| **MFA INFO.STATUS** | 字符串 `ENABLED / PENDING` | int `0/1/2`（DISABLE/ENABLE/PENDING_ACTIVE） | **改为字符串枚举且去掉 DISABLE**（决策 #5/#10）：仅 `ENABLED / PENDING` |
| **MFA 失败锁定** | >10 次 → 锁 60 分钟 | 同 | ✅ 一致 |
| **PARTNER USER REL.STATUS** | `ACTIVE / LOCKED` | `ACTIVE / EXPIRED / LOCKED` | **去掉 EXPIRED**（决策 #8）：仅 `ACTIVE / LOCKED`，到期由 AUTHORIZING TO 等字段组合派生 |
| **INVITE TOKEN 有效期** | 72 小时 | 7 天（`inviteTokenDays:7`） | **保留 7 天**（决策 #4，沿用现状） |
| **OPERATOR INVITE.STATUS** | 显式 `PENDING / CONSUMED / EXPIRED` | 无显式字段（由 `consumedAt`+`expiresAt` 派生） | 落地显式 status 字段（可保留派生兼容） |
| **INVITE 角色字段命名** | `INTENDED ROLE` / `AUTHORIZING TYPE` | mock 的 `inviteRoleIds` / `inviteAuthType` | 命名对齐 |

---

## 6. 🟡 PARTNER 实体本身

| 字段 | 新模型 | 现有代码 | 处理 |
|---|---|---|---|
| `EMAIL` | **实体直属字段** | 无；`getEntityView` 从首位 Admin 操作员派生 | 将 email 提升为实体持久字段，明确与操作员邮箱的关系 |
| `STATUS` | `ONBOARDING / ACTIVE` | `ONBOARDING / ACTIVE`（seed 已带） | ✅ 一致 |
| name/country/address/contactName/phoneCountryCode/phone/timezone/license/remark | 同 | 同 | ✅ 一致 |
| `tags`（≤15） | 未列 | 现有有 | 现有多出字段，新模型未要求删除，可保留 |

---

## 7. ✅ 基本一致、无需改动

- **PERMISSION CODE / Menu 死写在代码里** —— 与现有 `PERMISSION_CATALOG` / `MENU_TREE` 完全吻合。
- **DELEGATION（委托表）** —— 字段、`VIEW_ONLY / OPERATE` scope、全部约束（源契约须 ACTIVE MERCHANT、被委托方须 ACTIVE ISO、不能自委托、禁止再委托、源契约 TERMINATED 级联终止、`UNIQUE(sourceContractId + delegateeEntityId)`）逐条一致。
- **ROLE / ROLE BLOCKLIST** —— `roleType(global/private)`、`contractDefineCode`、`entityId` 约束、blocklist `UNIQUE(entityId+contractType+roleId)` 一致。
- **MERCHANT 仅由 ISO 创建 / ADMIN 全系统唯一** —— 约束一致。
- **DISTRIBUTOR 类型** —— 新模型列入；`data.jsx` 已有 `Distributor` 映射，仅 `data-tables.jsx` 的 `CONTRACT_TYPES` 漏列（既有内部不一致），借本次补齐。

---

## 8. 改造工作量分级（供排期参考）

| 等级 | 项 | 说明 |
|---|---|---|
| 🔴 大 | PILOT 类型化（§2） | 触及枚举、seed、事件、派生、契约 UI、转正流程 |
| 🔴 大 | MERCHANT entitlements（§3.1） | 新条款模型 + 编辑 UI + 计费口径 + seed 回填 |
| 🟠 中 | 契约事件词表统一（§4） | 枚举 + seed 重写 + 派生逻辑 |
| 🟠 中 | Entity→Partner 改名（§1） | 全局术语梳理（量大但机械） |
| 🟠 中 | ⏰ LOGOS（§3.3） | **本期不做**，延后；含上传组件 |
| 🟡 小 | USER 新增 DISABLED（§5） | 枚举 + 动作 + UI |
| 🟡 小 | MFA 状态改字符串（§5） | 编码映射 |
| 🟡 小 | INVITE 显式 status / 字段命名（§5） | 字段对齐 |
| 🟡 小 | PARTNER.email 提升为持久字段（§6） | 字段 + 派生关系 |
| 🟢 微 | AUTHORIZING PARTNER CONTRACT TYPE 冗余字段（§3） | 加字段 + 回填 |
| 🟢 微 | 枚举补 DISTRIBUTOR（§7） | 修内部不一致 |

> 沿用现状、无需改：密码历史 5 条、邀请 Token 7 天、ISO entitlements 形状、委托表、角色/黑名单、各约束。

---

## 9. 开放项（均已商定）

原开放项 A–D 已全部有结论，并合入 §0 决策表（#7–#10）：

- **A. LOGOS** → ⏰ **本期不做，延后**。其余改造完成后需主动提醒业务方重启。
- **B. UserRel `EXPIRED`** → **去掉**，改由其他字段组合派生。
- **C. PILOT 转正流程** → **采用建议方案**（终止 *-PILOT + 新建正式契约）。
- **D. MFA DISABLE 态** → **去掉**，仅 `ENABLED / PENDING`。

> ✅ 无剩余待决项。可据本文输出精确的代码改造清单。

---

## 10. 实施记录 · 第二轮（2026-06-04）—— 详版《PARTNER CONTRACT RELATIONSHIP》落地

> 起因：业务方提供了更细的《PARTNER CONTRACT RELATIONSHIP》定义。与**当时已改造过一轮的代码**（PILOT 已类型化、MERCHANT seed 已有、`settlementCurrency`/时区判断已具备）比对后，本轮落地以下增量。已与业务方确认：**DISTRIBUTOR 移除**、**LOGOS 仍延后**、其余全做。

### 10.1 🔴 ISO-PILOT entitlements 改为「独立形态」
- 新规则：`ISO-PILOT` 条款 = `deviceModels` + `settlementCurrency` + 四项增值服务**仅 `enable`（无价格）**，**无 `deviceBasicService`**。试用期全免费，价格概念不存在。
- 改动：
  - `customer-detail.jsx` 的 `CONTRACT_ENTITLEMENT_SCHEMA['ISO-PILOT']` 不再别名 ISO，改为独立 schema；增值项 `type:'toggle'`。
  - `clause-fields.jsx` 新增 `'toggle'` clause 类型（纯开关，`{ enable }`）+ 校验/读写转换。
  - seed `ec-015-iso` / `ec-016-iso`：删除 `deviceBasicService`，feature 收敛为 `{ enable }`。
  - `ISV-PILOT` 仍别名 ISV（无 entitlements）。

### 10.2 🔴 MERCHANT entitlements 编辑器补齐
- 新规则：MERCHANT 条款 = `settlementCurrency` + `PAYMENT SERVICE { enable, appItems:[{ pkgName, price/设备/月 }] }` + `E-RECEIPT SERVICE { enable, price/月, options:[] }`。`pkgName` ∈ {`com.newland.payexplorer`}；`options` ∈ {`EMAIL`,`QR CODE`}。
- 改动：
  - `clause-fields.jsx` 新增 `'payment'` / `'ereceipt'` 两个复合 clause 控件 + 校验 + 读写转换；导出 `PAYMENT_PKG_OPTIONS` / `ERECEIPT_OPTIONS`。
  - `customer-detail.jsx` `CONTRACT_ENTITLEMENT_SCHEMA.MERCHANT` 由 `[]` 改为完整 schema；read-only 展示新增 toggle/payment/ereceipt 的渲染分支。
  - seed `ec-007/008/010/013/014`：补顶层 `settlementCurrency`，去掉逐行 `currency`（非 USD 商户的币种展示此前会错回退成 USD，本轮修复）。

### 10.3 🟠 PILOT ↔ 正式契约 互斥 + 单向转正
- 新规则：同主体 `ISO` 与 `ISO-PILOT` 不可并存；先有正式 → 不可再建 pilot；先有 pilot 要转正 = 终止 pilot + 新建正式，且转正后不可恢复 pilot（ISV 同理）。
- 改动：
  - `shared.jsx` 新增 `contractFamily(kind)` 与 `pilotExclusionReason(kind, existing, tz)` 两个判定 helper。
  - `customer-wizard.jsx`：`toggleContract` 阻止同 family 二选一（toast 提示），删除"each type independent"的旧注释逻辑。
  - `customer-detail.jsx`：管理契约弹窗 `availableKinds` 用 `pilotExclusionReason` 过滤（pilot 在已有正式时隐藏、正式在 pilot 在效时隐藏 → 引导先终止/转正）；`onToggleMgmtPick` 加防御性 toast。

### 10.4 🟡 DISTRIBUTOR 移除
- `data-tables.jsx` `CONTRACT_TYPES`、`data.jsx` `CONTRACT_KIND_TO_LEGACY` / `CUSTOMER_CONTRACT_KINDS`、`shared.jsx` `iconForKind`、`index.html` 配色、各处注释中的 DISTRIBUTOR/Distributor 清除。无 seed 使用该类型，移除无数据影响。

### 10.5 🟡 ISO 正式 entitlements 形态收尾
- `deviceBasicService` → `{ price }`、feature → `{ enable, price }`、币种统一走顶层 `settlementCurrency`（脚本批量清理 7 条 deviceBasicService + 28 条 feature）。计费口径统一为**每项均按「设备/月」**（与详版 spec 一致；GeoFencing 仍含「依赖 GeoLocation」约束，本轮在 ISO + ISO-PILOT schema 上以 `requires:'GeoLocation'` 显式编码并在 UI 上联动禁用）。

### 10.6 ⏰ LOGOS —— 仍延后
- 详版 spec 再次完整列出 LOGOS（PLATFORM LOGO 数组 + APP BOOT LOGO，仅 ISO/ISO-PILOT 生效、含 md5），但按既定决策本期不做。**待其余收尾后提醒业务方重启。**

> 备注：契约 EVENT 词表（`BIND_PILOT`/`PILOT_EXTEND`/`PILOT_CONVERT` vs `CREATE_CONTRACT`…）本轮未动 —— 详版 spec 仅约束嵌套 `EVENTS` 数组的「绑定/挂起/恢复/终止」四类，现有 seed 的 `{statusFrom,statusTo,description}` 已满足；事件词表统一留作后续单独一轮。

---

_本文档为差异梳理与实施记录。§0–§9 为第一轮决策；§10 为本轮（详版契约关系）落地记录。_
