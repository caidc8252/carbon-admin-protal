# Carbon Admin Portal — UI 约定

本项目的 UI 公共规范（列表页 / 搜索栏 / 分页 / 高级搜索 / Active filters / 表单 / 详情页 / 框架 Shell / 内容布局 / Loading 等）**统一收敛到 `ui-spec/` 目录**，作为唯一事实来源。开始任何 UI 工作前先阅读它们，并以其为准。

## ⭐ 新建页面：优先遵循 `ui-spec/`（最高优先级）

**任何新建页面、新增屏幕、新做组件，都必须先读 `ui-spec/ui-spec.md` 并严格遵循其约定。** 当 `ui-spec/` 与其它来源（设计系统技能提示、历史代码、口头约定）冲突时，**一律以 `ui-spec/` 为准**。

### 样式架构：用本项目这套，不要用 `_ds/` bundle

新建时样式对不上，根因是**加载错了样式基座**。本项目实际运行的样式 = 下面三层，**不是** `_ds/` 设计系统 bundle：

1. **`assets/tokens.css`** — 全部设计 token（`--color-*` / `--space-*` / `--shadow-*` / `--control-height-*`），与 ui-spec 引用一致。
2. **`assets/components.css`** — 通用组件类（`tds-btn` / `tds-input` / `tds-select` …）。
3. **`index.html` 顶部内联 `<style>` + 各 JSX 文件注入样式** — 外壳/列表/标题栏/页面布局的真身（`.content` / `.page` / `.list-toolbar-wrap` / `.list-head` / `.side__*` / `.stepper` …）。

**约定：**
- 新建页面 **不要** 加载 `_ds/<folder>/_ds_bundle.js` 或 `_ds/` 下的任何 css —— 那是另一套基座，与 `assets/` 的 `.page` / `.content` 留白、间距不一致，**混用必然导致样式错位**。设计系统技能默认建议的 `_ds` 加载方式，在本项目作废。
- 新建页面 **挂进 `index.html` 这个 app**（在底部 `<script type="text/babel" src="…">` 列表追加并接入路由），而不是另建独立 HTML。
- **复用现有共享组件**，不要手拼裸 HTML：`shared.jsx`、`search-bar.jsx`、`titlebar.jsx`、`pagination.jsx`、`wizard-footer.jsx` 等。ui-spec 描述的列表卡 / 搜索栏 / 分页 / 标题栏 / 高级搜索 / Stepper 都已是封装好的组件，直接用。
- 颜色/字号/间距只用 `assets/tokens.css` 里的 `var(--*)`，不另造新值。

## 规范来源（`ui-spec/`）

- **`ui-spec/ui-spec.md`** — 规范正文。逐条列出全部约定，每条带状态标签：
  - **KEEP** 维持现状 · **NEW** 新增约束 · **FIX** 需修正现状
  - 覆盖：① 标题 & 页面外壳 · ② 新建/编辑表单 · ③ 列表卡 & 分页 · ④ 搜索栏样式 · ⑤ 高级搜索 · ⑥ Active filters · ⑦ 框架/Shell · ⑧ 详情页标题栏 & 返回按钮 · ⑨ Loading 加载态，外加内容布局与落地切入点附录。
- **`ui-spec/diagrams/`** — 配套示意图，按章节拆成若干可编辑 HTML（普通文件，双击即开）。入口 `ui-spec/diagrams/index.html`；各图共用 `_shared.css`（改样式改这一处）。与正文章节一一对应。

## 使用方式

1. **开始 UI 工作前**先读 `ui-spec/ui-spec.md`；需要直观参照时打开 `ui-spec/diagrams/index.html`（或对应章节的单张图）。
2. 规范以 **KEEP / NEW / FIX** 标注现状关系：实现新页面遵循全部条目；改造旧页面优先处理 **FIX** 项（见正文附录的落地切入点排序）。
3. `ui-spec/` 即唯一事实来源——若与代码现状或历史口头约定冲突，**以 `ui-spec/` 为准**，并在改造时对齐。

---

# 数据模型（`models/`）

各功能模块的数据模型定义（表结构、字段、约束）统一收敛到 `models/` 目录，作为模型层的唯一事实来源。`models/README.md` 是这些模型文档的**目录清单**（模块名 / 文件链接 / 一句话描述）。

## 查找 model 的规则

1. **先读 `models/README.md`**：它列出所有已登记模型及其文件入口和一句话描述，从这里定位要找的模块。
2. 再打开对应的 `models/<模块>.md` 读取完整字段与约束。
3. 任何涉及字段、枚举、约束、JSON 结构的实现或讨论，**以 `models/<模块>.md` 为准**；若与代码现状或历史口头约定冲突，以模型文档为准。

## 新增 model 的规则

1. 在 `models/` 下创建对应的 `<模块>.md`，文件名用小写英文模块名（如 `devices.md`、`merchants.md`、`orders.md`）。
2. **必须**同步在 `models/README.md` 的「模型清单」表格追加一行：模块名 / 文件链接 / 一句话描述。新增模型而不更新 README 视为未完成。
3. 模型文档沿用既有写法：按实体分 `##` 标题，字段用列表逐条列出（含类型/枚举/默认值/说明），约束统一放在该实体的 **约束** 小节下。

---

# 业务流程与规则（`flows/`）

各功能模块的**行为层**文档（业务规则、状态机、处理流程图/时序图、跨模块关联与后台处理、开放问题登记）统一收敛到 `flows/` 目录，作为行为层的唯一事实来源。它回答「**怎么动、动了之后还触发什么**」，与 `models/`（是什么）互补。`flows/README.md` 是目录清单（模块名/文件链接/一句话描述）；**`flows/_template.md` 是格式与写法明细的单一出处**——格式、ID 规则、确认状态标签定义、§1 九维探测清单、写作风格「反例→改法」清单都在那里；本节只保留**触发与判断要点**外加指向模板的指针，写文档前先读模板。

## ⭐ 捕获约定：在对话中边聊边沉淀（最重要）

flows 文档是**对话的沉淀物，不是前置产物**。无需用户提前备料——当对话中出现**业务规则 / 处理流程 / 状态流转 / 跨模块关联 / 后台处理**时，AI 应**主动**把它蒸馏进对应的 `flows/<模块>.md`：

0. **写全模块行为骨架,但详略分层**——目标是「不看原型也能读懂这个模块怎么动」,不是「只收原型表达不了的碎片」。三档处理:① **骨架简录**:状态机、生命周期、读懂行为必需的关键规则、跨模块触点——哪怕原型一眼可见,也用一句话点到,保证文档能独立读懂(落 **§2 业务规则**);② **隐性逻辑写深**:原型 / 事件流表达不了的(隐性业务逻辑、人工口述约定、跨模块副作用、后台处理)是高价值核心,写细、标好确认状态(落 **§1 后台处理与跨模块业务规则——全篇重点主场**);③ **真正不写**:纯 UI 细节(按钮何时显示、字段自动带出、表单怎么算、向导第几步)归原型,字段 / 枚举 / 约束归 `models/`。「原型能否表达」决定**写多深**、不决定**写不写**——别拿它当删除闸门(早前因此漏掉付款规则)。时序图 / 流程图**默认不画**,只在用户明确要求时补。
1. 用户说死的 → 标 **〔确认〕**；AI 合理补的 → **〔推断〕**；缺口 → 落成 `Q-` 开放问题并标 **〔待确认〕**。
2. **append 优先**：新认知就地加条目，改认知就地改那一条；ID 单调递增、永不复用。
3. **一条规则 = 一个完整业务块**（一个概念 / 一条链路），不是机械的「一句话」：可以稍长、可用子条目分点把同一条链路的几个环节列清。**宁可合并、不要把同一块拆成多条 RULE**（如「上传→扫描→审批→签名→下发」是一条链，写一条规则分点，不要拆四条）。**章节分工(以 §1 为重点主场)**:§1(后台处理与跨模块)写界面看不到的关联——写深写全、自带 RULE-ID、可被引用,**不是指针残桩**;§2(业务规则)写读懂模块必需的界面/本实体骨架,克制简录。**按重心整条归位、绝不为切章节把一个业务块拆两半**:重心在后台/跨模块的整条进 §1,重心在界面/本实体的整条进 §2;只有某条的后台尾巴本身够大、自成一条时,才在 §2 留「后台关联见 RULE-xx」、把尾巴独立成 §1 一条。§1 不重复 §2 已写明的可见规则;**§1/§2 共用一条 RULE 序列,编号与章节解耦**(条目挪章节 ID 不变)。每条带 ID + 确认状态（〔确认〕/〔推断〕/〔待确认〕）；涉及功能按钮时点名「哪个页面/哪个按钮」（界面位置，不是源码引用）。**用大白话写，少堆破折号与长串分句**：面向产品经理 / 业务读者，不是工程师。**禁止学术化、技术黑话与省略式行话**——宁可多用几个字把意思说透，也不要用一个让人要二次解释的词；专有名词第一次出现先用一句话解释再用。（常见「反例→改法」清单见 `flows/_template.md` §2 注释。）
4. **就关键不确定项发问,而非"全部非确认项逐条问完"。** 判据是**重要性**:猜错会改变开发结果 / 涉及钱、状态机分流、跨模块关联、数据留存的,必须问;纯措辞、低风险的合理推断,标〔推断〕留在文档里即可,不必逐条追问。用 questions_v2 把该问的**一批**摆出来、每项附"当前临时假设"做默认,答完就地把对应条目改〔确认〕、据答复修订正文、开放问题从表中移除。**带〔推断〕/开放问题的文档本身就可用**——不要为"凑齐确认"反复删档重推;append 优先、就地演进。文档顶部一句话标明哪些还开放即可,不必整篇判为"未定稿"。
5. 字段/枚举/约束类信息归 `models/`；flows 只用业务语言描述行为，**不重复定义字段、不写源码文件名/模型文件名/字段名**——下一道开发工序会同时拿到模型、原型与本文件，标来源是冗余。字段类信息冲突时以 `models/` 为准。
6. **以现有正确功能逻辑为准,不能只扫代码。** 原型 / 代码里常混有**历史废弃**的功能(废弃的入口、旧通知、停用的分支),光扫代码照单全收会把废功能写成规范。推导时:① 凡是「代码里有、但不确定现在是否仍在用」的功能,**先当可疑项、与用户确认再写**,不默认它有效;② 用 questions_v2 把可疑项连同"疑似废弃/疑似有效"的判断一起摆出来让用户裁决;③ 确认废弃的就**从文档移除**,不留痕。判据是**当前是否为有效设计**,不是**代码里是否存在**。

## ⭐ 深度推导工作法（产品经理视角，重点喂 §1）

flows 文档**最值钱的不是界面能推导的内容**——纯 CRUD 看 `models/`、界面流程看原型就够了。值钱的是**界面看不到、模型也表达不了的后台处理与跨模块关联**（落在 `flows/<模块>.md` 的 **§1**）。下面是把这层逻辑系统性挖出来的工作法。

**何时启动**（两个入口，都适用）：
- **问答共创时**：和用户边聊原型边推规则，对话里冒出后台/跨模块逻辑就当场蒸馏进 §1。
- **原型成型后反推**：原型/代码已经在了，对它做一次**系统性反推扫描**，把界面背后的逻辑挖出来补全 §1。

**四步走**：
1. **先划掉界面可见的。** 第一道动作是把"原型一眼能看懂的"排除掉——那些归 §2 骨架或干脆不写。火力集中到剩下的隐性逻辑。这道筛子是本工作法的组织核心。
2. **按探测清单逐维扫，出草稿。** 拿 `_template.md` §1 注释里那 9 个维度**逐项过一遍**原型，命中就落成 §1 的 RULE 块（先全标〔推断〕/〔待确认〕）。命中才写，未命中不写空条。每个隐性点先按第 0 条的三类判据归一类，别静默丢弃——尤其付款、对账、出款这类后台真实性，宁可落成 §5 开放问题也别漏。
3. **就关键不确定项批量问。** 用 questions_v2 把"猜错会改开发结果 / 涉及钱、状态机分流、跨模块关联、数据留存"的关键项**一批**摆出来，每项附"当前临时假设"做默认。低风险的合理推断标〔推断〕留在文档里即可，不必逐条追问。
4. **据答复就地改。** 答完把对应条目改〔确认〕、据答复修订正文、开放问题从 §5 移除。append 优先、就地演进——别为"凑齐确认"反复删档重推；带〔推断〕/开放问题的文档本身就可用。

## 维护规则

1. 新模块 → 复制 `flows/_template.md` 为 `flows/<模块>.md`（小写英文名），并在 `flows/README.md` 流程清单追加一行。
2. `flows/` 是**当前状态文档，不是变更流水账**：规则就地编辑，**不记 changelog**；历史需要时查文件版本历史。`README.md` 仅在新增/删除模块时变动，不累积细碎变更。
3. 涉及业务规则/流程/关联，与代码现状或历史口头约定冲突时，**以 `flows/<模块>.md` 为准**。
