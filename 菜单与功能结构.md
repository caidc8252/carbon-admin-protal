# Carbon Admin Portal — 菜单与功能结构

> 左侧导航的完整模块树:每个模块一句话概述 + 其下子功能。

---

## MANAGE 管理

### Customers 客户
管理客户公司及其合同、操作员。
- **客户列表** — 搜索 / 按合同类型、状态筛选,CSV 导出。
- **新建客户** — 分步向导(公司信息 → 合同 → 操作员)。
- **客户详情** — 公司资料、合同(Merchant / ISO / ISV / Pilot)、操作员账号。

---

## APPS 应用

### Published Apps 已发布应用
浏览已上架、可分发给客户的应用。
- **应用列表** — 状态(已发布 / 下架)、可见范围(全部 ISO / 指定 ISO)、分类筛选。
- **应用详情** — Overview / 版本 / 可见范围等 Tab。

### App Publish 应用发布
提交并发布新应用 / 新版本。
- **发布流程** — 上传、填写元数据、设置可见范围。

---

## SALES 销售

### Orders 订单
管理客户订单,从下单到发货的全流程。
- **订单列表** — 客户(含订单号)、类型、金额、状态、创建时间;排序与筛选。
- **订单详情(Overview)** — 状态流水线(Pending payment → Pending ship → Shipped → Delivered)、订单明细、金额(原价 / 折扣 / 合计)、客户与收货信息、Mark paid、Ship、Cancel(自动识别已付款 → 退款)。
- **订单详情(Devices & authorization)** — 发货时凭 6 位授权码把设备 SN 绑定到订单(写入设备表),逐台解绑;发货前校验全部绑定。

### Catalog 选购(前台下单)
面向销售的商品浏览页,加入购物车为客户下单。
- **分类标签栏** — 最多两级分类标签即时筛选 + 本页商品搜索。
- **商品详情(PDP)** — 图片、选项(Options)选择、数量、加入购物车 / 直接下单。
- **购物车** — 行项编辑、改选项、改价、数量。
- **结算向导** — ① 商品与数量 + 是否样机订单 + 折扣(按比例 / 按金额二选一)② 客户与收货 ③ 复核并创建订单。

### Products 商品(后台管理)
维护商品目录:分类、属性、SKU、上下架。
- **商品列表** — 按分类树筛选、状态(Active / Inactive)、搜索、CSV 导出。
- **商品表单** — 三步向导(Details → Attributes & pricing → Review & activate);属性取自所属分类,SKU 由选项组合生成,逐个 SKU 价格 + 可用开关。
- **Categories 分类管理** — 树型分类(可折叠);每个分类定义其商品可用的全部属性;属性来源:本类自定义 或 链接目标(如设备型号)只读同步。

---

## DEVICES 设备

### Device Models 设备型号
维护设备型号及其系统属性(供分类链接、商品取属性)。
- **型号列表** — 型号编码、属性。
- **型号详情** — 多值属性(存储、屏幕等)定义。

### Factory Images 出厂镜像
管理设备出厂固件镜像。
- **镜像列表** — 版本、适用型号。
- **新建镜像** — 分步向导。

### Devices 设备(库存 / 在网)
管理具体设备终端(SN 级)。
- **设备列表 / 详情** — 设备状态、归属、与订单 / 授权码的绑定关系。

---

## SUPPORT 支持

### Tickets 工单
客户支持工单,带生命周期状态轨。
- **工单列表** — 状态、处理人、时间。
- **工单详情** — 状态轨(New → Working → Replied → Closed)、往来记录。

---

## SYSTEM 系统

### Users 用户
平台内部用户账号管理。

### Roles 角色
内部角色与权限定义。

### Customer Role Definitions 客户角色定义
面向客户侧的角色模板定义。

### Audit Logs 审计日志
平台操作审计记录。
- **日志列表 / 事件详情** — 操作者、对象、时间、变更。

---

_说明:本结构对应左侧导航当前实现。Sales 三块的边界 —— **Catalog** 面向下单(前台、用 Options 措辞),**Products** 面向管理(后台、用 SKU / Attributes 措辞),**Orders** 贯穿下单到发货。_
