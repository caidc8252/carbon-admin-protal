# Carbon · 站内通知清单（v0.1 — 待评审）

> 目标：给右上角铃铛**只**保留运营必要的、可执行（actionable）的通知；其它一律不进通知中心。
> 范围：本文件同时覆盖 Carbon Admin（NPT 后台） / ISO 客户门户 / ISV 发布门户的通知模型，方便统一规划 `NotificationEvent` 的 topic。但**真实派发的通知列表**在每个平台铃铛里彼此独立。

图例：
- ✅ 用户已列出
- 🆕 建议补充（spec 明确要求 / 业务必要）
- ❓ 待业务方拍板

---

## 1. Ticket（工单）

| # | 通知 | 触发 | 收件人 | 备注 |
|---|---|---|---|---|
| ✅ | ISO 收到新工单 | ISO 自助门户产生新 ticket（`source: auto` 或 `manual`） | ISO 平台用户 | ISO 门户铃铛 |
| ✅ | 工单被提升到 NPT | ISO 把 ticket 升级，`status: new`，`nptHandler = 'NPT queue'` | NPT 全体 Support / Engineer | Carbon Admin 铃铛 |
| ✅ | 工单指派给我 | `Reassign` 把 `nptHandler` 设为当前用户（或 ISO 侧改 `isoOwner`） | 新 handler | Admin + ISO |
| ✅ | 我处理的工单有新评论 | comment 写入，`actor` 来自对方角色 | 当前 handler | Admin + ISO |
| 🆕 | **工单被转回队列** | 我手里的 ticket 被 Reassign 回 `NPT queue` | 原 handler | 需要知道"我不再负责了"，否则容易漏 |
| 🆕 | **关闭的工单被重新打开** | ISO reopen 或 NPT reopen，`closed → npt-working` | 上次 handler | 高优；很容易被遗漏的状态翻转 |
| 🆕 | **ISO 调整了 access grants** | `grants.{snapshot/logs/liveProbe/remoteDesktop}` 改动 | 当前 NPT handler | 影响 Workbench 工具可用性，必须通知，否则点工具会失败 |
| 🆕 | **NPT 已 Reply to ISO** | `status: npt-working → npt-resolved`，写 `resolution` timeline | ISO 侧 `isoOwner` | ISO 门户铃铛 |

❓ 工单 SLA 即将到期 —— 取决于是否引入 SLA 概念，spec 当前没写。

---

## 2. 客户邀请（操作员）

| # | 通知 | 触发 | 收件人 | 备注 |
|---|---|---|---|---|
| ✅ | 我生成的邀请链接已被接收 | `OperatorInvite.consumedAt` 写回 | `invitedBy` | |
| ✅ | 邀请链接即将过期（提前 1 天） | `inviteExpiresAt - now ≤ 24h` 且未消费 | `invitedBy` | |
| ✅ | 邀请链接已过期 | 过期未消费触发 invalid 终态 | `invitedBy` | |
| 🆕 | **首位操作员激活 → 客户进入 Active** | Entity 由 Onboarding → Active（spec §3.2.2 结尾） | 客户的 Account Owner / 创建该客户的 NPT 员工 | 这是 onboarding 完成的关键节点 |

---

## 3. 客户与契约（建议作为独立小分组，不放进"新建客户"下面）

> 你的清单里没有，但 spec §3.1.4 明确说 SUSPEND 会立即作废未消费邀请 + 阻断 Operator，这种全员级别的影响必须通知。

| # | 通知 | 触发 | 收件人 |
|---|---|---|---|
| 🆕 | **契约被新增 / Suspend / Resume / Terminate** | `EntityContract.status` 变更或 Add contract | 客户的 Account Owner + 持 `ctr.view` 的同事 |
| 🆕 | **客户被批量 Suspend all（派生 LOCKED）** | 所有 live 契约都进 SUSPENDED | 持 `cust.lock` / ops admin |
| 🆕 | **Merchant 契约被 ISO 委托 / 撤回委托** | `EntityContractDelegation` 新增或 TERMINATED | 双方 ISO + ops admin |

---

## 4. 应用订阅 / 发布（Apps）

> 这一组都是发生在 ISV / ISO 门户的通知，**不进 Carbon Admin 铃铛**，但要在统一通知模型里登记。

| # | 通知 | 触发 | 收件人 |
|---|---|---|---|
| ✅ | 我发布的应用被新 ISO 订阅 | App.subscriberCustomerIds 新增 | ISV 平台用户 |
| ✅ | 指定 ISO 的应用版本已发布 | `App.publishMode: private` 的版本上架 | 在 `specifiedIsos` 内的 ISO 平台用户 |
| ✅ | 我订阅的应用有新版本 | AppVersion.status: `unpublished → published` | 已订阅该 App 的 ISO 平台用户 |
| 🆕 | **应用版本扫描结果为 dirty** | `AppVersion.scan = 'dirty'` | NPT ops admin + 合规角色 + 该 App 的 publisher（ISV） | spec §4.2 有 `scan` 状态机，dirty 是合规风险，必须有人接手 |
| 🆕 | **Publisher 被 LOCKED（间接影响 App 可用性）** | publisher Entity 派生 LOCKED | 该 publisher 名下 App 的订阅 ISO | ISO 门户铃铛；否则 ISO 不知道为什么 App 突然不可用 |

---

## 5. 审核（你新引入的流程，spec 里没写）

> 你列了"修改 Email / 提升 Admin 权限"的审核流。spec v0.4 当前**没有这条流程**，建议先和业务方确认审核流的设计（谁审、几人审、审多久没回如何处理），再敲定通知。下表先按你给的版本列出，并补可能漏掉的环节。

| # | 通知 | 触发 | 收件人 |
|---|---|---|---|
| ✅ | 收到"修改 Email / 提升 Admin 权限"审核请求 | 申请提交 | 审核人（Platform Administrator） |
| ✅ | 我的申请被通过 / 拒绝 | 审核完成 | 申请人 |
| ✅ | 修改成功 → 通知客户当事人 | 审核通过后实际生效 | 客户侧当事人（ISO / ISV 平台用户） |
| 🆕 | **审核请求超时未处理 / 被撤回** | 申请提交后 N 天未审 → 提醒审核人；申请人主动撤回 → 通知审核人 | 审核人 |
| 🆕 | **同一个对象有多条 pending 审核** | 同一 Entity / User 上叠加未决申请 | 申请人 + 审核人 | 防止重复申请打架 |

❓ 审核流到底覆盖哪些动作？建议先把范围明确，例如：
- 修改 Email
- 提升 Admin 权限 / 调整 ADMIN 角色权限点
- 解锁敏感操作（如 `data.export` 全量导出？`workbench` factory reset？）

---

## 6. 订单 / 设备

| # | 通知 | 触发 | 收件人 |
|---|---|---|---|
| ✅ | 订单完成 | `Order.status: → Complete` 或样机激活回写 | 订单创建者 |
| 🆕 | **订单付款已确认（可以备货发货）** | 计费回调 `paid` → `Awaiting payment → Awaiting shipment` | 订单创建者 + 履约对接人 | 没有这条通知，下游不知道什么时候开始发货 |
| 🆕 | **履约 / 激活回调失败** | `ship` / `deliver` / 设备激活回调异常 | ops admin | 异常路径必须有提醒，否则订单会卡死无人发现 |

❓ 设备激活码 5 分钟过期 / 自动重生 —— 通常只在订单详情打开时展示，不进铃铛；除非业务方明确希望被提醒。

---

## 7. 安全与审计（spec §4.3 明确要求，强烈建议补）

| # | 通知 | 触发 | 收件人 |
|---|---|---|---|
| 🆕 | **敏感字段被 reveal**（`data.revealed`） | 任一员工触发 `data.reveal` | 持 `data.reveal` 或 `audit.view` 的角色 | **spec §4.3 明文要求的通知**，目前清单里完全没有，必须补 |
| 🆕 | **平台员工账号被锁定** | NPT User 连续登录失败 → LOCKED | Platform Administrator | 高优；安全事件 |

---

## 8. 个人 / 系统（最小集）

| # | 通知 | 触发 | 收件人 |
|---|---|---|---|
| 🆕 | **我被邀请加入 Carbon Admin** | 平台员工邀请发出 | 当事人 | 没有这条，新人不知道自己被邀请了 |
| 🆕 | **我的平台角色被变更** | `EntityUserRelationship.roleIds` 调整且当事人是 NPT 员工 | 当事人 | 权限变化必须显式告知 |

---

## 全局约定建议（与通知列表无关，但影响铃铛设计）

1. **红点 badge** = 未读 **高优** 通知数；普通优先级进列表但不计入红点。
2. **分类 Tab**：`All / Tickets / Customers & Orders / Audit / System`。
3. **去重折叠**：同一 ticket / 订单短时间内多条事件折叠为一行 "X new updates on T-2026-0123"。
4. **权限可见性**：通知项受权限点过滤（如 `data.revealed` 只对持 `data.reveal` / `audit.view` 的人可见）。
5. **行为**：点击 = 标记已读 + 跳转；忽略 = 仅标记已读；"Mark all as read"。
6. **不做**：营销、推荐、"您可能感兴趣"类。Carbon 是内部运营工具，每条通知都必须能点出一个明确的下一步动作。

---

## 待业务方确认（在动手做铃铛 UI 之前最好定下来）

- **Q1** 审核流覆盖范围？只 Email + Admin 权限，还是更多敏感动作？
- **Q2** 是否引入 ticket SLA？有则需要"即将到期"通知。
- **Q3** "通知客户当事人"具体走哪条通道？站内信（在客户门户铃铛）/ 邮件 / 两者都发？
- **Q4** ISV / ISO 平台的铃铛是不是这次也要一起设计，还是只先做 Carbon Admin？
- **Q5** 通知保留期？是否分"未读永久 / 已读 30 天"两档？
